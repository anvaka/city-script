{
  "version": "0.5.0",
  "name": "city-script",
  "description": "A set of simple scripts that can be loaded to https://github.com/anvaka/city-roads",
  "scripts": {
    "build": "browserify -s city index.js > dist/city-script.js",
    "start": "http-server -c-1 --cors"
  },
  "main": "index.js",
  "jsdelivr": "dist/city-script.js",
  "dependencies": {
    "ngraph.graph": "^19.1.0",
    "ngraph.path": "^1.3.1",
    "ngraph.random": "^1.1.0"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "http-server": "^0.12.3"
  }
}
